int missingNumber(int A[], int N)
{
    
    int sum = ((N*(N+1))/2);
    
    int total = 0;
    
    for(int i=0;i<N-1;i++){
        total = total + A[i];
    }
    
    int ans = sum - total;
    
    return ans;
    
}
