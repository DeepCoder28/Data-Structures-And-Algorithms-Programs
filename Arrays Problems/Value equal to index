vector<int> valueEqualToIndex(int arr[], int n) {
	    
	    vector<int>a;
	    for(int i=0;i<n;i++){
	        if(arr[i] == i+1){
	            a.push_back(arr[i]);
	        }
	    }
	    
	    return a;
	}
